<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/style.css">
    <title>My anime list</title>
</head>
<body>
    
    <%- include('./includes/header.ejs') %> 

    <main class="container d-flex gap-3 flex-wrap justify-content-center">


        <% for(let i = 0; i < animes.length; i++) {%> 

            <div class="card d-inline-block">
                <img class="card-image-top img-fluid" src="<%= animes[i].imgUrl %>" alt="">
                <div class="card-body">
                    <h5 class="card-title"><%= animes[i].title %> </h5>
                </div>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item anime-info">Score: <%= animes[i].score %> </li>
                    <li class="list-group-item anime-info"><%= animes[i].situation %> </li>
                    <li class="list-group-item anime-info">

                        <% if (animes[i].comments) { %>
                            <%= animes[i].comments %> 
                        <% } else { %>
                            No comments 
                        <% } %> 

                    </li>
                </ul>


                <div class="card-body d-flex">
                    <a href="/edit-anime/<%= animes[i].id %>?edit=true" class="btn text-warning">Edit</a>

                    <form action="delete-anime" method="POST">
                        <input type="hidden" name="animeId" value="<%= animes[i].id %> ">
                        <button class="btn text-danger">Delete</button>
                    </form>
                </div>

            </div>

        <% } %> 

    </main>

    <script src="/scripts/main.js"></script>
</body>
</html>